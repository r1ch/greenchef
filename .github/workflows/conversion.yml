

name: PDF to JPG

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install ghostscript
        run: sudo apt install ghostscript
      - name: Convert PDF to PNG
        run: find raw/*.pdf -exec sh -c 'gs -dNOPAUSE -dBATCH -sDEVICE=png256 –dFirstPage=1 –dLastPage=1 -r100x100 -sOutputFile="${1%.*}-thumb.png" "$1"' sh {} \;
      - name: Move thumbs pages
        run: mv raw/*.png converted
      - name: Clear old pngs
        run: rm raw/*.png
      - name: Commit JPG
        id: commit
        run: |
          git config --local user.email "action[bot]@github.com"
          git config --local user.name "github-actions[bot]"
          git add raw/*
          git add converted/*
          if [-z "$(git status --porcelain)"]; then
              echo "Nothing to do"
          else
             git commit -m "[bot] updated JPG files"
             git push
          fi
        shell: bash
